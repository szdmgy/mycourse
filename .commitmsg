优化课程导入体验：重复检测、名单变动提示、布局重构

- 预览增加名单变动检测：区分新增/退出/不变学生，已存在且无变化的课程禁止重复导入
- 已存在课程有名单变动时，按钮改为「确认更新名单」并显示具体变动详情
- write_course_data 支持自动移除退出学生（不在新 Excel 中的学生）
- 预览页面布局重构：汇总和操作按钮上移至课程信息下方，学生名单默认折叠
- 退出学生单独列表展示，教师和学生详情列表均可折叠展开
- confirm_import 成功消息区分新建和更新两种场景
