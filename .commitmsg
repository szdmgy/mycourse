多线程并行密码哈希：批量导入从 33 秒降至约 5 秒

- 诊断发现瓶颈是 make_password（PBKDF2 87万次迭代，每次 425ms）
- 数据库写入仅 1 秒，密码哈希占 97% 耗时
- 新增 _bulk_make_passwords 函数，利用 ThreadPoolExecutor 并行哈希
- 自动检测 CPU 核心数，最多使用 16 线程
- write_student_users 和 write_teacher_users 均改用并行哈希
