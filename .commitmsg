修复作业导入：Excel 文件内重复标题导致写入失败

- parse_task_excel 增加文件内去重检测（之前只检查数据库重复）
- 区分两种重复：已存在于数据库(dup_reason=db) / 文件内重复(dup_reason=file)
- 前端预览表格区分显示：红色「文件内重复-跳过」和黄色「已存在-跳过」
- confirm_task_import 增加异常捕获，返回 JSON 错误而非 HTML 500 页面
