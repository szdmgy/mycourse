# mycourse 变更记录

> 本文档汇总仓库内**已实现且已纳入版本控制**的功能与修复，便于部署与验收对照。  
> 最新一次合订：**2026-07-21**（批改参考分）

---

## 2026-07-21 批改参考分（可选）

- `HomeworkGrade.score`：可选 0–100 整数参考分；仅教师可见，不参与 F 判定
- 提交记录批改页：等级旁窄输入框；单条/批量保存支持；Excel 明细增加「参考分」列
- API 写入可选 `score`；省略则保留原值，`null`/空串清空；迁移 `0011_homeworkgrade_score`
- 说明文档：教师版批改专节、学生版成绩可见性、README / API / 部署速查同步

---

## 2026-07-17 历史复用同步预检配置

- 「从历史复用」除报告模板外，同步复制作业级 `precheck_mode` / `precheck_fail_mode` / 框架规则包
- 列表标「含预检」（非纯继承默认时）；「继承课程」仍跟随目标课程总开关

---

## 2026-07-16 成绩汇总：占比分母与饼图

- 等级占比分母改为应交总人数；Excel 同步
- 页面底部增加人数比例饼图（本地 SVG）；未批改/未提交为 0 时不单独显示

---

## 2026-07-16 预检仅警告：保留警告标记

- 「仅警告」确认后仍提交：Homework 记录警告内容，师生端列表/提交页显示「预检警告」
- 重新提交且预检全部通过（或未开启预检）后自动清除；迁移 `0010_homework_precheck_warn`

---

## 2026-07-16 封面预检：实验时间 / 提交时间

- 封面有「实验时间」「实验报告提交时间」时做内置日期校验（宽松格式，至少年月）
- 实验时间与今天相差不超过约半年；实验时间 ≤ 提交时间；提交时间 ≤ 截止日
- 规则包声明 `skip_builtin_cover_dates` / `cover_dates=custom` 或 rule 带 field 指向这两项时，跳过内置日期规则

---

## 2026-07-16 报告预检（封面 + 框架 API）

- 课程总开关：关闭 / 默认封面预检；失败策略硬拦截或仅警告
- 继承默认时：**无报告模板不预检**；有模板才做封面预检。作业可专用开启封面/框架
- 框架预检：JSON DSL 规则包，API `GET|PUT|DELETE /api/v1/tasks/<id>/precheck/`
- 学生提交时预检；硬拦截拒绝；警告可确认后重传（precheck_ack）
- 师生端展示：教师作业列表「预检」列；学生作业列表「预检」列；提交页顶部预检说明
- 迁移 `0009_precheck_settings`

---

## 2026-07-16 全面禁止旧版 Word（.doc）

- 学生提交、教师模板上传、作业允许类型配置均拒绝 `.doc`（须用 `.docx`）
- 前后端双校验，错误时弹窗提示

---

## 2026-07-16 标准封面字段规则 + 仅 docx 可用模板

- 自动填写：课程名称←系统课程名（覆盖模板原值）；学院/专业固定；指导教师/报告人/学号/班级←系统；实验时间与提交时间不改
- 报告模板仅当作业允许 `.docx` 时可用；纯 zip 等作业不可挂模板、学生端不显示下载

---

## 2026-07-16 模板封面格式修复 + 课程默认模板

- 封面自动填写改为按 run 改字，保留原有下划线等格式
- 课程详情支持「课程默认报告模板」：全课共用一份；作业可上传专用模板覆盖
- 学生下载解析：作业专用优先，否则用课程默认；迁移 `0008_course_default_report_template`

---

## 2026-07-16 报告模板 + 封面自动填写

- 作业级完整 `.docx` 模板；目录 `file/<学期>/<课程班>/报告模板/<作业标题>/`（与参考资料分离）
- 教师「编辑作业」页：上传/替换/删除、下载母版、开关（允许下载 / 自动填封面）；上传后默认双开
- 课程作业列表显示模板状态；历史复用实验时**同步复制**模板文件与开关（列表标「含模板」）
- 学生：作业列表与提交页「下载报告模板」；开启自动填时写入姓名/学号/班级（占位符 + 空白封面行）
- API：`GET/PATCH /api/v1/tasks/<id>/` 含模板字段与开关；`POST|DELETE /api/v1/tasks/<id>/template/` 上传/删除
- 依赖：`python-docx`；迁移 `0007_task_report_template`

---

## 2026-07-15 批改体验增强

- 作业记录：一键保存全部修改、筛选（未批改/F/待重评）、顶栏批改进度迷你摘要
- 成绩汇总页 /gradeSummary/<taskID>/：进度、等级分布与占比、名单下钻；支持导出 Excel
- API：GET /api/v1/tasks/<id>/grades/summary/

---

## 2026-07-15 F 提醒强弱分流

- F 且未重交：红标 + 红色顶部警示 + 每会话弹窗；成绩计 F
- F 且已重交（待重评）：黄标「已重交·待重评」+ 黄色弱提醒横幅，不弹窗；成绩仍计 F
- 教师改非 F 后警示消失

---

## 2026-07-15 定性批改

- 模型 `HomeworkGrade`：等级 A+/A/B/C/D/F、可选评语、`needs_regrade`；迁移 `0006_homework_grade`
- 教师「作业记录」页可打分保存；学生端仅 **F** 可见（强弱提醒见上节）
- F 后重交保留 F 并标记待重评
- API：`/api/v1/tasks/<id>/grades/`、`/api/v1/homeworks/<id>/grade/`（需 API Key）

---

## 2026-07-15 在线预览（LibreOffice → PDF）

- 新增 pp01/libreoffice_preview.py：自动发现项目内 LibreOfficePortable，将 .doc/.docx 转为 PDF 并缓存于 previews/
- 路由：/preview-homework-file/<file_id>/（登录；本人或教师/管理员）
- .pdf 直接内联打开；Word 转 PDF 后浏览器新标签预览
- 入口：学生提交页、教师作业记录、课程详情提交列表
- .gitignore 增加 LibreOfficePortable/、previews/

---

## 2026-07-15 第一阶段（P0 + 超管身份切换）

### 1. 提交时间拆分
- Homework.time 拆为 submitted_at（首次）与 updated_at（最后更新）
- 逾期判定改为按 **首次提交日期** 相对 deadline
- 上传重传时正确刷新 updated_at
- 迁移：pp01/migrations/0005_homework_timestamps_and_impersonation.py（已有数据回填）

### 2. API Key 鉴权 + 作业设置 API
- 环境变量 MYCOURSE_API_KEY（.env / .env.example）
- GET|PATCH|PUT /api/v1/tasks/<id>/ 需 Bearer 或 X-API-Key
- 旧 GET /api/v1/submission-status/ 暂不强制鉴权，响应增加 submitted_at / updated_at（submit_time 仍为首次，兼容旧调用方）
- 文档：mycourse/mycourse/docs/API说明.md

### 3. 超级管理员身份切换
- 顶栏「切换身份」：按姓名/学号搜索、课程筛选（可过滤）、角色、最近切换
- 顶栏警告横幅 + 一键退出；禁止切换其他超管
- 审计模型 ImpersonationLog
- 路由：/impersonate/search|start|stop/

### 4. 设计说明
- docs/design-phase1.md


---

## 2026-04-27 合订说明

### 1. 参考资料（课程级静态文件）

- **模型**：`app01.ReferenceMaterial`，字段含课程、标题、说明、`filePath`、`originalName`、`file_size`、`sort_order`、`display`、上传者、时间戳。
- **存储路径**：`file/<学期>/<课程名+班号>/参考资料/`，与作业目录同树；**磁盘文件名与上传 basename 一致**，不自动加随机后缀。
- **校验（预检）**：文件名非法（含 `<>:"|?*/\`、路径分隔符、Windows 保留名等）或**本课程内同名文件（忽略大小写）**或目录已存在同名物理文件时，**拒绝上传/不入库**。
- **教师端**：课程详情 Tab「参考资料」— 上传、编辑（可换文件）、删除、下载、**上移/下移**排序；**从同名课程其他学期/班级复制**（含磁盘文件副本，权限对齐 `getHistoryTasks`）。
- **学生端**：「我的课程 → 课程」Tab「参考资料」，仅见 `display=True` 的条目，可下载。
- **权限**：`can_manage_course`（管理员任意课程；教师须为课程 `members`）；下载按角色校验。
- **迁移**：`0003_reference_material.py`、`0004_reference_material_sort_order.py`。

### 2. 作业相关 URL 修复

- `taskSubmit`、`homeworkRecords`、`taskChange` 路由**仅保留 `taskID`**，不再把作业标题拼进 URL，避免标题含 `/` 等导致路由错误。
- 模板中 `{% url %}` 已同步调整。

### 3. Windows 部署/升级脚本（对齐 expense_log / 全局 Y12）

- 根目录 **`mycourse_deploy_update.bat`**：首次克隆 + `venv` + 依赖；二次运行检查 `.env` 与 `db.sqlite3` 后 **`git pull --ff-only`**、依赖、`migrate`、`collectstatic`。
- 适配本仓库 **Git 根目录与 `manage.py` 分离**（`mycourse/mycourse/`）。
- **`mycourse/mycourse/.env.example`**：占位说明；升级时若无 `.env` 可尝试从示例复制。
- 文档：**`docs/服务器部署升级速查.md`**；**`README.md`** 增加部署/升级说明。

### 4. 文档

- **`docs/参考资料-功能需求.md`**：参考资料产品规则与验收要点（含「纯前端演示已移除」说明）。
- 本文档 **`docs/变更记录.md`**。

### 5. 仓库维护

- **`.gitignore`**：增加 `.cursor/`、`db-old.sqlite3`，避免误提交本地备份与编辑器目录。

---

## 历史版本（摘录，见 `git log`）

| 日期（约） | 说明 |
|------------|------|
| 此前提交 | 学生作业提交状态 API、请求日志中间件、`show_routes`、学生课程列表按学期分组等 |

更细粒度以 **`git log`** 为准。
